<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <style>
        /* Basic reset for consistent styling */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Header styling */
        header {
            background: linear-gradient(90deg, #22c55e, #0f766e); /* Gradient green for a modern look */
            padding: 20px;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Adds subtle depth */
        }

        /* Navigation styling */
        nav {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px; /* Consistent spacing between links */
        }

        /* Navigation links styling */
        nav a {
            color: #ffffff; /* White text for contrast */
            text-decoration: none;
            font-size: 18px;
            font-weight: bold;
            padding: 10px 20px; /* Adds padding for clickable area */
            border-radius: 5px;
            transition: all 0.3s ease; /* Smooth animations for hover effects */
        }

        /* Hover effect for links */
        nav a:hover {
            background-color: #065f46; /* Light green background on hover */
            color: #fff; /* Ensure text remains white on hover */
            transform: scale(1.1); /* Slight scaling effect */
        }

        
    </style>
</head>
<body>
    <header>
        <nav>
            <a href="/" >Home</a>
            <a href="/application">Registration</a>
            <a href="/visualization">Data</a>
        </nav>
    </header>
</body>
</html>
